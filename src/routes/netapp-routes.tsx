export const NETAPP_PREFIX = '/netapp';
export const STORAGE_ROOT = `${NETAPP_PREFIX}/storage`;
export const STORAGE_BACKEND = `${STORAGE_ROOT}/backend`;
export const STORAGE_VOLUMES = `${STORAGE_ROOT}/volumes`;